# Audit Infrastructure Assets

- `apigw-access-log-format.json` – JSON log template applied to the `prod` stage of `b3yr01g4hh`.  
- `apigw-access-log-patch.json` – Pre-baked patch operations file for use with `aws apigateway update-stage`.

Both files are referenced in `docs/audit/AUDIT-PIPELINE.md`.

